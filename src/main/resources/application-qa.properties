# QA Configuration
spring.datasource.url=jdbc:postgresql://qa-db-host:5432/ecommerce?currentSchema=ecom
spring.datasource.username=qa_user
spring.datasource.password=qa_password

# JPA / Hibernate
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate

# Logging
logging.level.com.ksl.template=INFO
